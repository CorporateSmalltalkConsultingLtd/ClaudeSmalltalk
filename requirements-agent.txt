# Python dependencies for Smalltalk Agent
# Requires Python 3.10+

# Required for all setups (used by all providers: Anthropic, OpenAI, xAI, Ollama)
httpx>=0.25.0

# Optional — only if transport.type is "mqtt"
# paho-mqtt>=2.0.0
